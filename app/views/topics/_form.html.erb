<div class="container">
  <%= form_for(@topic) do |f| %>
  <% if @topic.errors.any? %>
  <div id="error_explanation" class="white-color">
    <h2><%= pluralize(@topic.errors.count, "error") %> prohibited this topic from being saved:</h2>

    <ul>
      <% @topic.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
  <% end %>
  <div class="control-group white-color">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="controls white-color">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions white-color">
    <%= f.submit :class => "btn btn-primary", :value => "Post New Activity" %>
  </div>
</div> <!-- /container -->
<% end %>
