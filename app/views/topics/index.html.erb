<div class="container">
<script>
  var end = new Date('02/14/2015 9:00 AM');

  var _second = 1000;
  var _minute = _second * 60;
  var _hour = _minute * 60;
  var _day = _hour * 24;
  var timer;

  function showRemaining() {
    var now = new Date();
    var distance = end - now;
      if (distance < 0) {
        clearInterval(timer);
        document.getElementById('topicPageCountDown').innerHTML = 'VOTING IS OVER JERK FACES';
        return;
      }
        var days = Math.floor(distance / _day);
        var hours = Math.floor((distance % _day) / _hour);
        var minutes = Math.floor((distance % _hour) / _minute);
        var seconds = Math.floor((distance % _minute) / _second);

        document.getElementById('topicPageCountDown').innerHTML = days + 'days ';
        document.getElementById('topicPageCountDown').innerHTML += hours + 'hrs ';
        document.getElementById('topicPageCountDown').innerHTML += minutes + 'mins ';
        document.getElementById('topicPageCountDown').innerHTML += seconds + 'secs' + " To Jamjua Day!";
      }

      timer = setInterval(showRemaining, 1000);
    </script>
  <div id="topicPageCountDown"></div>

</div>
<table>
  <thead>
    <tr>
      <h2><em>Welcome to Jammit</em></h2>
        </tr>
      </thead>  

      <tbody>
      <%= link_to 'Create New Jammit', new_topic_path, class: "btn btn-default" %>
      <% @topics.each do |topic| %>
      <tr>
        <td><h4><%= link_to topic.title, topic %></h4></td>
        <td><div class="paddingLeft"><h5><%= pluralize(topic.votes.count, "vote") %></div></h5></td>
        <td><div class="upvote"><%= link_to image_tag('upvote.png'), upvote_topic_path(topic), method: :post %></div></td>
        <td><div class="upvote"><%= link_to image_tag('downvote.png'), downvote_topic_path(topic), method: :delete, data: { confirm: "Downvotes are mean guize..." } %></div></td>
        <td><%= link_to 'DELETE ACTIVITY', topic, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
      </tbody>
    </table>
    <div class="spacing"></div>
  <em>you're disgusting</em>
  <div class="spacing"></div>
</div> <!-- /container -->
